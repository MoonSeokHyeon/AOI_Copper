<UserControl x:Class="MVision.UI.InteractivityViews.View.NotificationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVision.UI.InteractivityViews.View"
             mc:Ignorable="d" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Width="450" Height="200">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="0.2*" />
            <RowDefinition Height="1.5*" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <TextBlock Style="{DynamicResource MaterialDesignHeadline5TextBlock}">Message</TextBlock>

        <TextBlock x:Name="Message" Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0 6 0 0" Text="{Binding Message}" FontSize="18"/>

        <Grid Grid.Row="1">
            <Separator Background="{DynamicResource PrimaryHueMidBrush}"/>
        </Grid>

        <Button Grid.Row="3" IsDefault="True" Style="{DynamicResource MaterialDesignFlatButton}" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="16 16 16 0" Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}">
            ACCEPT
        </Button>
    </Grid>

</UserControl>
